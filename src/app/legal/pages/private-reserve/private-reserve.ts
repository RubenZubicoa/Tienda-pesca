import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-private-reserve',
  imports: [RouterLink],
  templateUrl: './private-reserve.html',
  styleUrl: './private-reserve.scss',
})
export class PrivateReserve {}
